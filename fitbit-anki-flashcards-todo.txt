(A) Initialize Fitbit SDK project via `npx create-fitbit-app ankivert`, set package.json `requestedPermissions: ["access_internet","run_background","access_user_profile"]`, targets: `["watch","companion","settings"]`, fitbit SDK `"^1.10.0"` +scaffold
(A) Define directory structure: `app/` (watch JS), `companion/` (phone JS), `settings/` (JSX), `resources/svgs/` (layouts), `common/` (shared constants), `resources/images/` (icons) +scaffold
(A) Create `common/constants.js` exporting MSG_TYPES: `CARDS_CHUNK`, `CHUNK_ACK`, `SYNC_COMPLETE`, `RATINGS_REQUEST`, `RATINGS_PAYLOAD`, `SETTINGS_UPDATE`; export SM2_GRADES: `AGAIN=0`, `HARD=2`, `GOOD=3`, `EASY=4` +scaffold
(A) Configure `package.json` build scripts: `"build": "fitbit-build"`, `"sideload": "npx fitbit"`, set `wipeColor: "#0d0d1a"`, `appDisplayName: "AnkiVert"` +scaffold

(A) Build Settings login section in `settings/index.jsx`: render `<Section label="AnkiWeb Login">` containing `<TextInput settingsKey="ankiweb_user" label="Username"/>`, `<TextInput settingsKey="ankiweb_pass" label="Password" type="password"/>`, `<Button label="Connect" onClick={sendLoginRequest}/>`, `<Text>` showing current auth status from `props.settings.auth_status` +auth
(A) In `companion/index.js`, listen for `settingsStorage.onchange` event on key `ankiweb_pass`; on change, POST `application/x-www-form-urlencoded` body `username=<u>&password=<p>` to `https://sync.ankiweb.net/hostKey`, read plain-text response as hostKey string +auth
(A) On successful hostKey response: store `localStorage.setItem("ankiweb_host_key", hostKey)` and `localStorage.setItem("ankiweb_key_expiry", Date.now() + 86400000)` (24hr TTL), send `settingsStorage.setItem("auth_status", "Connected")` +auth
(A) On 403 response from `/hostKey`: send `settingsStorage.setItem("auth_status", "Invalid credentials")`, clear stored key; on network error set status `"Connection failed"` +auth
(A) On Companion initialize: read `localStorage.getItem("ankiweb_host_key")` and `ankiweb_key_expiry`; if key absent or `Date.now() > expiry`, set `auth_status` to `"Session expired — reconnect"` and abort sync; otherwise proceed to deck fetch +auth
(A) Add `<Button label="Logout" onClick={sendLogout}/>` in Settings `<Section label="AnkiWeb Login">`; Companion handles `SETTINGS_UPDATE {loggedOut:true}` by clearing `ankiweb_host_key`, `ankiweb_key_expiry`, `available_decks`, `selected_decks` from localStorage +auth

(A) In Companion, fetch deck names: POST `https://sync.ankiweb.net/sync/deckNames` with header `Authorization: hostKey <token>`, parse JSON array of deck name strings, store as `localStorage.setItem("available_decks", JSON.stringify(names))` +sync
(A) Handle deck fetch 401: clear stored key, set `auth_status` to `"Session expired"`, abort; 5xx: set `settingsStorage.setItem("sync_error", "AnkiWeb unavailable")` and abort; timeout (10s): set `"Sync timed out"` and abort +sync
(A) Render deck selection in `settings/index.jsx`: map `JSON.parse(props.settings.available_decks || "[]")` to `<Toggle settingsKey={\`deck_\${deckName}\`} label={deckName}/>` inside `<Section label="Sync These Decks">` +settings
(A) Render queue size control in Settings: `<Slider settingsKey="queue_size" label="Cards per Session" min={10} max={200} step={10}/>` inside `<Section label="Queue Settings">`, default value 50 +settings
(A) Render last sync timestamp in Settings: `<Text>Last synced: {props.settings.last_sync_ts ? new Date(parseInt(props.settings.last_sync_ts)).toLocaleString() : "Never"}</Text>` inside `<Section label="Status">` +settings
(A) Render sync error field in Settings: if `props.settings.sync_error` is non-empty show `<Text style={{fill:"red"}}>{props.settings.sync_error}</Text>` and `<Button label="Retry Sync" onClick={retrySyncCmd}/>` +settings
(A) Add `<Button label="Sync Now" onClick={triggerManualSync}/>` in Settings Status section; Companion receives `settingsStorage.onchange` on key `manual_sync_trigger`, runs full sync pipeline on receipt +settings

(A) Derive selected decks in Companion: iterate `Object.keys(settingsStorage)` filtering keys starting with `"deck_"`, collect deck names where value `=== "true"`, return as string array +sync
(A) Fetch due card IDs per selected deck: POST `https://sync.ankiweb.net/sync/cards` with body `{hostKey, decks: selectedDecks, dueOnly: true}`, parse response JSON array of card ID strings; merge arrays from all selected decks, deduplicate via `Set` +sync
(A) Fetch full card data in batches of 50: POST `https://sync.ankiweb.net/sync/cardsInfo` with `{hostKey, ids: batch}`, parse JSON array of objects with fields `{id, front, back, noteType, interval, easeFactor, repetitions, due}` +sync
(A) Filter cards to supported note types: keep only cards where `noteType === "Basic"` or `noteType === "Cloze"`; discard all others silently +sync
(A) Strip media from `front` and `back` strings: remove `[sound:*]` via `/\[sound:[^\]]+\]/g`, remove `<img[^>]+>` via `/<img[^>]+>/gi`, strip remaining HTML tags via `/<[^>]+>/g`, trim whitespace +sync
(A) Render Cloze card front: replace all cloze markers via `/\{\{c\d+::([^:}]+)(?:::[^}]*)?\}\}/g`; for c1 ordinal replace with `[...]` (or `[hint]` if hint group present), for all other ordinals replace with the answer text directly (reveal non-tested clozes) +sync
(A) Render Cloze card back: replace all cloze markers via same regex, always substituting `$1` (the answer group), returning fully revealed text +sync
(A) Truncate `front` to 120 chars: if `front.length > 120` set `front = front.slice(0, 117) + "..."` after all formatting steps +sync
(A) Truncate `back` to 200 chars: if `back.length > 200` set `back = back.slice(0, 197) + "..."` after all formatting steps +sync
(A) Enforce queue size cap: sort due cards by ascending numeric `due` field, slice to `Math.min(queueSize, cards.length)`, where `queueSize = parseInt(settingsStorage.getItem("queue_size") || "50")` +sync
(A) Shuffle same-day due cards to avoid monotony: within cards sharing the same `due` value, apply Fisher-Yates shuffle before slicing to queue cap +sync
(A) Serialize final card payload as JSON string: `JSON.stringify([{id, front, back, type, interval, easeFactor, repetitions}])` store in Companion `localStorage.setItem("pending_cards", payload)` and update `last_sync_ts` +sync
(A) Schedule automatic background sync: on Companion initialize, call `setInterval(() => { const h = new Date().getHours(); if(h === 6) runSync(); }, 3600000)` to trigger daily at ~06:00 local time +sync
(A) Run sync automatically when `messaging.peerSocket` opens for first time after Companion initialize (watch newly connected): call `runSync()` then immediately begin card transfer if cards available +sync

(A) Implement chunked card transfer from Companion to Watch: JSON string → split into 1000-char chunks, send each as `messaging.peerSocket.send({type:"CARDS_CHUNK", index:n, total:N, data:chunk})`, wait for `CHUNK_ACK` before sending next chunk (sequential, not concurrent) +transfer
(A) Implement per-chunk ACK timeout in Companion: after sending chunk, set 3s timeout; if `CHUNK_ACK` not received with matching `index`, retransmit same chunk; abort after 3 consecutive retransmit failures and set `settingsStorage.setItem("sync_error", "Transfer failed — reconnect watch")` +transfer
(A) In Companion `messaging.peerSocket.onmessage`: dispatch on `msg.type`; `CHUNK_ACK` → advance chunk index and send next chunk; `RATINGS_PAYLOAD` → process ratings array; route unknown types to no-op +transfer
(A) Guard Companion sends with `messaging.peerSocket.readyState === messaging.peerSocket.OPEN`; if not open, push transfer job to `pendingTransfer` flag and initiate on next `peerSocket.onopen` event +transfer
(A) In Watch `messaging.peerSocket.onmessage`: dispatch on `msg.type`; `CARDS_CHUNK` → chunk buffer handler; `RATINGS_REQUEST` → read ratings.json and reply; `SYNC_COMPLETE` → clear ratings.json; unknown → no-op +transfer
(A) Implement chunk reassembly on Watch: maintain `let chunkBuffer = new Array(msg.total)` initialized on first chunk (index 0); set `chunkBuffer[msg.index] = msg.data`; send `{type:"CHUNK_ACK", index:msg.index}`; when all slots filled, call `onCardsReceived(chunkBuffer.join(""))` +transfer
(A) Handle `messaging.peerSocket.onerror` in Watch: set `errorText.text = "Sync failed — open phone app"`, switch display to `nosync-screen` if `cardQueue.length === 0` +transfer

(A) In Watch `onCardsReceived(jsonStr)`: parse JSON, validate it is an array; write to `/private/data/cards.json` via `fs.writeFileSync("/private/data/cards.json", jsonStr, "ascii")`; reload `cardQueue` from parsed array; if currently on `nosync-screen`, render first card front +storage
(A) On Watch initialize: attempt `fs.readFileSync("/private/data/cards.json", "ascii")`; if file absent (throws), initialize `cardQueue = []` and render `nosync-screen`; if present, parse JSON into `cardQueue` and render first card front +storage
(A) On Watch initialize: attempt `fs.readFileSync("/private/data/ratings.json", "ascii")`; if absent, write `"[]"` to create it; store parsed array in `pendingRatings` in-memory array +storage
(A) Implement `appendRating(ratingObj)`: push to in-memory `pendingRatings`, write full array back via `fs.writeFileSync("/private/data/ratings.json", JSON.stringify(pendingRatings), "ascii")` +storage
(A) On receipt of `RATINGS_REQUEST` from Companion: read `/private/data/ratings.json`, send `{type:"RATINGS_PAYLOAD", ratings: parsedArray}` via `messaging.peerSocket.send()` +storage
(A) On receipt of `SYNC_COMPLETE` from Companion: overwrite `/private/data/ratings.json` with `"[]"`, reset in-memory `pendingRatings = []` +storage

(A) Create `app/sm2.js`: export function `grade(card, q)` where `q ∈ {0,2,3,4}` (AGAIN/HARD/GOOD/EASY); if `q < 2`: set `reps=0, interval=1`; else: if `reps===0` set `interval=1`, else if `reps===1` set `interval=6`, else set `interval=Math.round(card.interval * card.easeFactor)`; compute `ef = Math.max(1.3, card.easeFactor + 0.1 - (5-q)*(0.08+(5-q)*0.02))`; if `q >= 2` increment `reps`; return `{interval, easeFactor: ef, repetitions: reps}` +sm2
(A) Write `app/sm2.test.js` (run via `node app/sm2.test.js`): assert new card GOOD → `{interval:1, repetitions:1}`; second GOOD → `{interval:6, repetitions:2}`; third GOOD → `{interval:~12, repetitions:3}`; AGAIN on reps=2 card → `{interval:1, repetitions:0}`; EASY on new card EF check EF increases from 2.5 +sm2
(A) After grading: build `ratingObj = {id:card.id, rating:q, timestamp:Date.now(), newInterval:result.interval, newEaseFactor:result.easeFactor, newRepetitions:result.repetitions}` and call `appendRating(ratingObj)` +sm2

(A) Create `resources/svgs/index.gui.xml`: root `<svg viewport-fill="#0d0d1a" width="336" height="336">`; define four named `<symbol>` elements: `id="front-screen"`, `id="back-screen"`, `id="complete-screen"`, `id="nosync-screen"`; render one `<use>` referencing active symbol +device-ux
(A) Design `front-screen` symbol: `<text id="progressLabel" x="168" y="22" text-anchor="middle" font-size="14" fill="#666">` for "X / Y" counter; `<text id="cardFront" x="168" y="80" text-anchor="middle" font-size="22" fill="#e8e8e8">` allowing up to 4 `<tspan dy="1.3em" x="168">` children for multiline; `<text x="168" y="316" text-anchor="middle" font-size="13" fill="#555">TAP TO REVEAL</text>` +device-ux
(A) Design `back-screen` symbol: `<text id="cardBack" x="168" y="50" text-anchor="middle" font-size="19" fill="#e8e8e8">` allowing up to 5 `<tspan dy="1.2em" x="168">` children; four rating zones each a `<rect>` + `<text>` label: Again (fill="#8b0000" x=0 y=210 w=168 h=126 label="Again"), Hard (fill="#7a4f00" x=0 y=210... +device-ux
(A) Define exact rating zone layout for `back-screen`: split bottom 130px (y=206 to y=336) into 2x2 grid; top-left (0-168, 206-271): Hard "#5c4a00"; top-right (168-336, 206-271): Good "#0a4a0a"; bottom-left (0-168, 271-336): Again "#4a0000"; bottom-right (168-336, 271-336): Easy "#003d5c"; each zone has centered label `<text fill="white" font-size="16">` +device-ux
(A) Design `complete-screen` symbol: centered `<text y="130" font-size="26" fill="#e8e8e8">Session Complete</text>`; `<text id="statsReviewed" y="180" font-size="18" fill="#aaa">`; `<text id="statsAgain" y="210" font-size="18" fill="#aaa">`; `<text y="290" font-size="14" fill="#555">Sync phone for more cards</text>` +device-ux
(A) Design `nosync-screen` symbol: `<text y="150" text-anchor="middle" x="168" font-size="22" fill="#666">No cards synced</text>`; `<text y="185" text-anchor="middle" x="168" font-size="15" fill="#444">Open AnkiVert on phone</text>` +device-ux

(A) In `app/index.js`, import `document` from `"document"`, `fs` from `"fs"`, `messaging` from `"messaging"`, `display` from `"display"`, `sm2` from `"./sm2"`, `{MSG_TYPES, SM2_GRADES}` from `"../common/constants"`; declare module-level `let cardQueue=[], currentIndex=0, sessionStats={reviewed:0, again:0}, pendingRatings=[]` +device-ux
(A) Implement `renderFront(index)` in `app/index.js`: extract `card = cardQueue[index]`, call `setMultilineText(cardFrontEl, card.front, 4)`, set `progressLabel.textContent = \`\${index+1} / \${cardQueue.length}\``, switch active symbol to `front-screen` +device-ux
(A) Implement `setMultilineText(el, text, maxLines)`: clear existing `<tspan>` children, split text into lines of 22 chars each via `text.match(/.{1,22}/g)`, slice to `maxLines`, append `<tspan dy="1.3em" x="168">line</tspan>` per line via `document.createElementNS` +device-ux
(A) Implement `renderBack(card)`: call `setMultilineText(cardBackEl, card.back, 5)`, switch active symbol to `back-screen` +device-ux
(A) Implement `renderComplete()`: set `statsReviewed.text = \`Reviewed: \${sessionStats.reviewed}\``, set `statsAgain.text = \`Again: \${Math.round(sessionStats.again/sessionStats.reviewed*100)||0}%\``, switch to `complete-screen` +device-ux
(A) Register `document.getElementById("front-screen").addEventListener("click", () => renderBack(cardQueue[currentIndex]))` for card flip gesture +device-ux
(A) Register click listeners on each rating zone rect by ID (`zone-again`, `zone-hard`, `zone-good`, `zone-easy`): call `handleRating(cardQueue[currentIndex], SM2_GRADES.AGAIN/HARD/GOOD/EASY)` respectively +device-ux
(A) Implement `handleRating(card, grade)`: compute `result = sm2.grade(card, grade)`, build `ratingObj` with id/grade/timestamp/result fields, call `appendRating(ratingObj)`, update in-memory card's interval/easeFactor/repetitions, increment `sessionStats.reviewed`, if grade===0 increment `sessionStats.again`, advance `currentIndex`, if `currentIndex >= cardQueue.length` call `renderComplete()` else call `renderFront(currentIndex)` +device-ux
(A) Implement swipe-left skip on `front-screen`: add `touchstart` listener storing `startX`; add `touchend` listener computing `dx = endX - startX`; if `dx < -60` splice card from `currentIndex`, push to end of `cardQueue`, re-call `renderFront(currentIndex)` without recording rating; do not decrement `cardQueue.length` +device-ux
(A) Register `display.onchange = () => { if(display.on === false) { /* save currentIndex to /private/data/state.json */ } else { /* restore currentIndex on wake */ } }` to persist session position across display sleep +device-ux
(A) On app initialize: read `/private/data/state.json` if exists, restore `currentIndex` if `< cardQueue.length`; delete state file after restoring to avoid stale state on next cold launch +device-ux

(A) Process `RATINGS_PAYLOAD` in Companion: parse `msg.ratings` array; for each rating, POST to `https://sync.ankiweb.net/sync/updateCard` with body `{hostKey, cardId:r.id, interval:r.newInterval, easeFactor:r.newEaseFactor, repetitions:r.newRepetitions, due: ankiDayFromTimestamp(r.timestamp + r.newInterval*86400000)}` +ratings-sync
(A) Implement `ankiDayFromTimestamp(ms)` in Companion: `return Math.floor((ms - new Date("2006-01-01T00:00:00Z").getTime()) / 86400000)` matching Anki's internal day counter +ratings-sync
(A) Collect failed rating POSTs (network error or 5xx) into `failedRatings` array; after processing all, store `localStorage.setItem("failed_ratings", JSON.stringify(failedRatings))` for retry on next sync +ratings-sync
(A) On each Companion sync run start: load `failed_ratings` from localStorage, prepend to current ratings batch before POSTing, clear `failed_ratings` after confirmed success +ratings-sync
(A) After all ratings POSTed successfully: send `{type:MSG_TYPES.SYNC_COMPLETE}` to Watch, update `settingsStorage.setItem("last_sync_ts", Date.now().toString())`, clear `settingsStorage.setItem("sync_error", "")`, then immediately run card fetch for next session +ratings-sync
(A) After new cards fetched and serialized post-ratings: begin chunked transfer to Watch automatically without requiring manual trigger +ratings-sync

(A) (STRETCH) Add `<Section label="Sync Mode">` in Settings JSX with `<Toggle settingsKey="use_anki_connect" label="Use AnkiConnect (Local Anki Desktop)"/>` and conditionally rendered `<TextInput settingsKey="anki_connect_url" label="AnkiConnect URL" placeholder="http://192.168.1.x:8765"/>` when toggle is true +anki-connect
(A) (STRETCH) In Companion sync dispatcher: read `settingsStorage.getItem("use_anki_connect")`; route to `runAnkiConnectSync()` if `"true"`, else `runAnkiWebSync()` +anki-connect
(A) (STRETCH) Implement `fetchDeckNamesAnkiConnect(url)`: POST `{"action":"deckNames","version":6}` to `<url>`, return `response.result` string array; on fetch error (connection refused) surface "Open Anki desktop with AnkiConnect plugin" in Settings +anki-connect
(A) (STRETCH) Implement `fetchDueCardsAnkiConnect(url, decks)`: for each deck POST `{"action":"findCards","version":6,"params":{"query":"is:due deck:\"<deckName>\""}}` collect ID arrays; then POST `{"action":"cardsInfo","version":6,"params":{"cards":[...ids]}}` in batches of 50 +anki-connect
(A) (STRETCH) Map AnkiConnect `cardsInfo` fields to unified schema: `front=card.question`, `back=card.answer`, `easeFactor=card.factor/1000`, `interval=card.interval`, `repetitions=card.reps`, `type=card.note.includes("Cloze")?"cloze":"basic"`, apply same strip/truncate pipeline +anki-connect
(A) (STRETCH) Implement `pushRatingsAnkiConnect(url, ratings)`: map rating objects to `{cardId: r.id, ease: [1,1,2,3,4][r.rating]}` (Again=1, Hard=2, Good=3, Easy=4 in AnkiConnect 1-4 scale); POST `{"action":"answerCards","version":6,"params":{"answers":[...]}}` in batch +anki-connect
(A) (STRETCH) Handle AnkiConnect `response.error` non-null: log error, surface in `settingsStorage.setItem("sync_error", response.error)`, retain ratings for retry +anki-connect

(A) (STRETCH) Port Companion `companion/index.js` to Android: verify `messaging` and `settingsStorage` APIs behave identically under Android Fitbit Companion SDK; if Keychain not available on Android, confirm `localStorage` suffices for hostKey storage (Android Companion JS sandbox) +android
(A) (STRETCH) On Android, test chunked transfer throughput: if BLE throughput lower than iOS causing chunk ACK delays, increase chunk ACK timeout from 3s to 6s via platform detection `if(navigator.userAgent.includes("Android"))` +android

(A) Create 80x80px app icon as `resources/images/icon.png`: dark navy background (#0d0d1a), white stylized "A" glyph (Anki-inspired) centered, export at 72dpi +gallery
(A) Create 480x270px feature banner `resources/images/banner.png`: show watch mockup with front-screen card displaying sample Japanese vocab card, dark background +gallery
(A) Verify `package.json` fitbit manifest: `appId` must be a valid UUID v4 (generate via `node -e "console.log(require('crypto').randomUUID())"`), `iconFile: "resources/images/icon.png"`, `i18n.en.name: "AnkiVert"`, `i18n.en.description: "Anki spaced repetition flashcards on your wrist"` +gallery
(A) Run `npx fitbit-build` and confirm zero errors; check output `.fba` bundle size is under 10MB (Fitbit Gallery limit); fix any SVG syntax errors or missing resource refs reported by build +gallery
(A) Sideload for device testing: run `npx fitbit`, then `build` then `install` in Fitbit CLI REPL; confirm watch app renders `nosync-screen` on first launch, Settings page loads and shows login fields on phone +gallery
(A) Perform end-to-end test: login with AnkiWeb credentials → verify deck list populates → select 1 deck → tap Sync Now → verify cards transfer to watch → review 5 cards with mixed ratings → reopen companion → verify ratings sync back to AnkiWeb card schedules +gallery
(A) Submit to Fitbit App Gallery via developer.fitbit.com: attach `.fba`, set category "Health & Fitness", upload icon and banner, provide privacy policy URL (must state no user data stored on servers beyond AnkiWeb's own servers), submit for review +gallery
