(A) Initialize Go module `redact-cli` with Cobra CLI framework, define root command with `--config`, `--log-level`, `--verbose` global flags +cli
(A) Set up Python package `redact-pipeline` with pyproject.toml containing deps: spacy, transformers, pytesseract, pikepdf, python-docx, Pillow +pipeline
(A) Define IPC protocol between Go and Python: Go spawns Python subprocess, communicates via JSON-over-stdin/stdout with message types: `ocr_request`, `ocr_result`, `ner_request`, `ner_result`, `error` +ipc
(A) Implement Go-side IPC client: spawn Python `redact-pipeline` as child process, send JSON messages, read JSON responses with configurable timeout (default 120s per document) +ipc
(A) Implement Python-side IPC server: read JSON from stdin in loop, dispatch to OCR or NER handler, write JSON results to stdout, handle graceful shutdown on EOF +ipc
(A) Create config file schema at `~/.config/redact-cli/config.toml` with sections: `[ocr]` (engine, language), `[ner]` (model_path, confidence_threshold), `[redaction]` (default_mode), `[patterns]` (jurisdiction) +cli
(A) Implement `redact-cli configure` subcommand: interactive prompts via Bubble Tea to set OCR engine, NER model path, default redaction mode, jurisdiction, and write to config.toml +cli
(A) Implement file type detection module in Go: inspect magic bytes and file extension to classify input as PDF, DOCX, or image (PNG/JPG/TIFF), return enum type +ingest
(A) Implement PDF text extraction in Python using `pikepdf` and `pdfminer.six`: extract text with positional metadata (page number, bounding box coordinates x0,y0,x1,y1) per text span +pdf
(A) Implement PDF page-to-image rasterization in Python using `pdf2image` (poppler backend): convert each PDF page to PNG at 300 DPI for OCR fallback on image-heavy PDFs +pdf
(A) Implement scanned PDF detection heuristic: if extracted text from a page has fewer than 50 characters but page has visual content, flag page as needing OCR +pdf
(A) Implement Tesseract OCR module in Python: accept image bytes, run `pytesseract.image_to_data()` to get word-level text with bounding box coordinates and confidence scores +ocr
(A) Implement olmOCR module in Python as optional backend: download model on first use to `~/.cache/redact-cli/models/`, accept image bytes, return text with bounding boxes in same schema as Tesseract output +ocr
(A) Create OCR engine abstraction in Python: factory function accepting engine name string ("tesseract" | "olmocr"), returning unified interface with `extract(image_bytes) -> List[TextSpan]` where TextSpan has text, bbox, confidence, page_num +ocr
(A) Implement automatic OCR engine selection: use Tesseract by default, fall back to olmOCR if Tesseract confidence is below 0.6 average across a page, log engine choice +ocr
(A) Implement DOCX text extraction in Python using `python-docx`: extract paragraphs and runs with structural metadata (paragraph index, run index, style info) preserving original formatting context +docx
(A) Implement DOCX table text extraction: iterate all tables in document, extract cell text with row/column indices and cell bounding context +docx
(A) Implement DOCX header/footer extraction: extract text from all header and footer elements across sections +docx
(A) Download and load pretrained spaCy model `en_core_web_trf` as base NER model, wrap in class `LegalNERModel` with `predict(text) -> List[Entity]` returning entity text, label, start_char, end_char, confidence +ner
(A) Define entity taxonomy enum: PERSON, ORGANIZATION, ADDRESS, PHONE, EMAIL, SSN_US, NATIONAL_ID, FINANCIAL_ACCOUNT, DATE_OF_BIRTH, CASE_NUMBER, CUSTOM — used across all NER and pattern modules +ner
(A) Implement regex-based PII pattern matcher in Python: accept list of compiled regex patterns with entity labels, run against text, return matches in same Entity schema as NER output +patterns
(A) Create US jurisdiction pattern set: SSN (XXX-XX-XXXX), EIN, US phone formats, US address patterns (state abbreviations + zip), US bank routing numbers +patterns
(A) Create UK jurisdiction pattern set: National Insurance Number, UK phone formats, UK postcode patterns, NHS number +patterns
(A) Create EU jurisdiction pattern set: IBAN, EU VAT numbers, common EU phone formats, EU date formats (DD/MM/YYYY) +patterns
(A) Create generic pattern set: email addresses (RFC 5322 regex), IPv4/IPv6 addresses, credit card numbers (Luhn-validated), dates in multiple formats +patterns
(A) Implement jurisdiction config loader: read `[patterns].jurisdiction` from config (e.g., "us", "uk", "eu", "generic"), load corresponding pattern sets, support comma-separated list for multi-jurisdiction (e.g., "us,generic") +patterns
(A) Implement user-defined custom pattern support: read `[patterns].custom_patterns_path` pointing to a TOML file where users define regex patterns with labels, load and merge with jurisdiction patterns +patterns
(A) Build entity merger module in Python: combine NER model output and regex pattern matches, deduplicate overlapping spans (prefer higher confidence), sort by document position +ner
(A) Implement confidence threshold filtering: accept `--min-confidence <float>` flag (default 0.7), filter out entities below threshold, pass borderline entities (0.5-0.7) as "needs review" +ner
(A) Implement NER fine-tuning pipeline script `finetune_ner.py`: accept spaCy-format training data JSON, fine-tune `en_core_web_trf` on legal entity examples, save model to configurable output path +ner
(A) Create sample legal NER training data generator: script that produces 200 synthetic legal document snippets with annotated entities using Faker library for realistic PII generation +ner
(A) Implement Bubble Tea TUI main application model: define app states (FILE_SELECT, PROCESSING, REVIEW, REDACT_CONFIG, EXPORTING, BATCH_SUMMARY), handle state transitions +tui
(A) Build file/directory picker view in TUI: display filesystem tree rooted at working directory, navigate with j/k/arrows, Enter to select file, Tab to toggle directory expansion, support multi-select with Space +tui
(A) Implement batch directory selection: when user selects a directory, recursively discover all PDF/DOCX/image files within, display count and file list for confirmation before processing +tui
(A) Build processing view: show per-file progress with spinner, current stage (extracting text → running OCR → detecting entities), file name, and overall batch progress bar (N/M files) +tui
(A) Build entity review view: split-pane layout — left side shows document text with highlighted entities (color-coded by type), right side shows entity list with type, text, confidence, and action toggle +tui
(A) Implement entity action controls in review view: for each detected entity, user can toggle: REDACT (default for high confidence), SKIP (keep as-is), EDIT (modify entity boundaries), cycle with Space/Enter +tui
(A) Implement entity boundary editing: when user selects EDIT on an entity, allow expanding/shrinking the highlighted text span using Shift+Left/Right arrows, confirm with Enter +tui
(A) Implement entity type override: Tab on a selected entity cycles through entity taxonomy types, allowing user to reclassify (e.g., change ORGANIZATION to PERSON) +tui
(A) Add "Redact All of Type" bulk action: keybinding `a` opens type selector, choosing a type marks all entities of that type as REDACT +tui
(A) Add "Skip All Below Confidence" bulk action: keybinding `c` prompts for confidence threshold via inline input, marks all entities below threshold as SKIP +tui
(A) Build redaction mode selector view: before applying redaction, prompt user to choose mode — "shallow" (black box overlay, fast, reversible) or "deep" (text stream removal, thorough, irreversible) with explanation text +tui
(A) Implement shallow PDF redaction in Python using `pikepdf`: overlay black rectangles at entity bounding box coordinates on each page, preserve original text streams underneath +redact
(A) Implement deep PDF redaction in Python using `pikepdf`: locate text stream operators (Tj, TJ, ', ") containing entity text, surgically remove matching character sequences from content streams, then overlay black rectangle +redact
(A) Implement deep PDF redaction post-processing: after text stream removal, rewrite PDF object streams to eliminate any residual references to redacted text in metadata, bookmarks, and XMP data +redact
(A) Implement X-Ray compatibility check for deep redaction: after redaction, re-extract text from redacted regions using pdfminer — if any redacted text is still recoverable, log warning and re-process that region +redact
(A) Implement DOCX shallow redaction: replace entity text spans in paragraph runs with black-highlighted replacement characters (e.g., "████████") preserving run formatting and paragraph structure +docx
(A) Implement DOCX deep redaction: replace entity text in runs with redaction placeholder, then strip revision history (w:rsid attributes), comments referencing redacted ranges, and tracked changes containing redacted text +docx
(A) Implement image-based document redaction: for scanned images/image-only PDFs, draw filled black rectangles at OCR bounding box coordinates on the rasterized image, re-embed into PDF +redact
(A) Build export options view in TUI: after redaction decisions confirmed, show checkboxes for output formats — redacted PDF, redacted DOCX, redaction manifest JSON — with output directory picker +tui
(A) Implement redacted PDF output: write redacted PDF to `<original_name>_redacted.pdf` in user-selected output directory +export
(A) Implement redacted DOCX output: write redacted DOCX to `<original_name>_redacted.docx` preserving original styles, images, and non-redacted content +export
(A) Implement redaction manifest JSON output: write sidecar `<original_name>_manifest.json` containing: source filename, redaction mode, timestamp, list of redacted entities (type, original_text_hash_sha256, page/location, confidence, action taken) +export
(A) Ensure manifest never contains actual redacted plaintext: store only SHA-256 hash of original entity text for audit trail, never the raw PII +export
(A) Implement batch summary view in TUI: after all files processed, show table with filename, entity count, redactions applied, output paths, any warnings/errors +tui
(A) Wire end-to-end single file flow: file select → text extraction → OCR (if needed) → NER + patterns → entity review → redaction mode select → apply redaction → export +cli
(A) Wire end-to-end batch flow: directory select → discover files → sequential processing with per-file TUI review → batch summary with aggregated statistics +cli
(B) Implement redaction replacement text options: instead of solid black, allow configurable replacement strings per entity type (e.g., PERSON → "[PERSON_1]", "[PERSON_2]" with consistent numbering across document) +redact
(B) Implement entity cross-referencing: detect when same entity text appears multiple times in document, link occurrences so redacting one auto-marks all as REDACT with same replacement label +ner
(B) Add search within review view: `/` activates text search in document, highlights matches, useful for finding entities the model missed +tui
(B) Implement manual entity addition in review view: keybinding `n` enters selection mode where user highlights text span and assigns entity type, added to entity list as user-defined +tui
(B) Add `redact-cli validate <file>` subcommand: run X-Ray-style analysis on an already-redacted PDF, report any recoverable text under black boxes or in metadata +cli
(B) Implement NER model hot-swap: `redact-cli model set <path>` updates config to point to a custom fine-tuned model, validate model loads correctly before persisting +cli
(B) Add `redact-cli patterns list` subcommand: print all active patterns for current jurisdiction config with examples of what each matches +cli
(B) Add `redact-cli patterns test <text>` subcommand: run configured patterns against provided text string, show matches with highlights — useful for pattern authoring +cli
(B) Implement undo/redo in entity review view: `u` to undo last action (redact/skip/edit), `Ctrl+r` to redo, maintain action stack per document +tui
(B) Add confidence distribution visualization in review view: show histogram/sparkline of entity confidence scores in status bar to help user gauge model quality +tui
(B) Implement document page navigation in review view: PageUp/PageDown to jump between pages, status bar shows current page / total pages +tui
(B) Add "high confidence auto-redact" mode: entities above user-defined threshold (e.g., 0.95) are auto-marked REDACT, only entities below threshold presented for review — reduces review burden on clean documents +tui
(B) Implement parallel OCR processing: for multi-page documents, OCR pages concurrently using Python multiprocessing pool (default: CPU core count - 1) +ocr
(B) Add PDF/A output compliance: when exporting redacted PDFs, optionally produce PDF/A-compliant output for long-term archival using pikepdf's PDF/A writing mode +export
(B) Implement dry-run mode: `--dry-run` flag processes document through NER/patterns but skips redaction, outputs manifest JSON only showing what would be redacted +cli
(B) Add India jurisdiction pattern set: Aadhaar number (12 digits with Verhoeff checksum), PAN (XXXXX0000X format), Indian phone (+91), IFSC code, Indian PIN code +patterns
(B) Add Australia jurisdiction pattern set: TFN (Tax File Number), ABN, Medicare number, Australian phone formats, Australian postcode +patterns
(B) Add Canada jurisdiction pattern set: SIN (Social Insurance Number), Canadian phone, Canadian postal code (A0A 0A0), provincial health numbers +patterns
(B) Implement pattern set auto-detection heuristic: scan first 2000 chars of document for jurisdiction-specific indicators (country names, legal citation styles, currency symbols), suggest applicable jurisdiction +patterns
(B) Add `redact-cli export-config` subcommand: dump current effective config (merged defaults + config file + CLI flags) as TOML to stdout for debugging +cli
(B) Implement keyboard shortcut help overlay in TUI: `?` key shows all available keybindings for current view, dismiss with Esc +tui
(B) Add color theme configuration: `[tui].theme` in config supporting "dark" (default), "light", "high-contrast" with appropriate color palettes for entity type highlighting +tui
(B) Implement session autosave: during entity review, periodically save review state (entity actions) to `~/.cache/redact-cli/sessions/<hash>.json`, restore if same file reopened +tui
(B) Add DOCX embedded image handling: detect images within DOCX documents, run OCR on embedded images, include extracted text entities in review +docx
(B) Implement redaction statistics logging: after each file, append entry to `~/.local/share/redact-cli/redaction_log.json` with anonymized stats (file hash, entity counts by type, redaction mode, duration) +export
(C) Add multi-language OCR support: configure Tesseract language packs via `[ocr].languages` config (e.g., "eng+fra+deu"), download packs on first use +ocr
(C) Implement NER model evaluation script `evaluate_ner.py`: accept held-out test set, compute precision/recall/F1 per entity type, output confusion matrix +ner
(C) Add side-by-side before/after view in TUI: split pane showing original text on left, redacted preview on right, synchronized scrolling +tui
(C) Implement regex pattern testing playground in TUI: accessible via `redact-cli patterns playground`, live-test regex against sample text with match highlighting +patterns
(C) Add PDF form field redaction: detect and optionally redact filled form fields (AcroForm) in PDFs containing PII +pdf
(C) Implement redaction certificate: generate signed JSON document attesting to redaction completeness, including tool version, model version, and entity coverage statistics +export
(C) Add DOCX comment and tracked-changes stripping as pre-processing step: remove all comments and revision marks before NER to prevent PII leakage through markup +docx
(C) Implement incremental review for large documents: process and review entities page-by-page rather than loading entire document into memory, reducing RAM usage for 500+ page documents +tui
(C) Add PDF digital signature preservation: warn user if input PDF is digitally signed (signature will be invalidated by redaction), offer to proceed or abort +pdf
(C) Implement entity annotation export: export entity review decisions as spaCy-format training data JSON, enabling users to improve NER model with their corrections over time +ner
