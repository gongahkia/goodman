(A) Set up Python project with Textual, PyTorch, SQLAlchemy, sdv (Synthetic Data Vault), and Faker as core deps +core
(A) Define core domain types: SchemaTable{name, columns[]}, Column{name, dtype, nullable, pk, fk_ref, constraints}, ForeignKey{src_table, src_col, dst_table, dst_col} +core
(A) Define SyntheticConfig{table, row_count, method(gan|copula|faker|tvae), seed, fk_mode(auto|manual|skip)} per table +core
(A) Define GenerationResult{table_name, rows_generated, duration, method_used, privacy_score} +core
(A) Build SQLAlchemy-based schema introspector: reflect all tables, columns, types, PKs, FKs, unique/check constraints from live DB +schema +db
(A) Implement PostgreSQL connection handler with connection string parsing and SSL support +db +postgres
(A) Implement DDL SQL file parser: extract CREATE TABLE statements and parse into SchemaTable objects without live DB connection +schema +parser
(A) Map SQL types to synthetic data categories: numeric(int/float/decimal), text(varchar/text/char), temporal(date/datetime/timestamp), boolean, uuid, json, enum +schema +types
(A) Detect semantic column types by name heuristics: columns matching *email* -> email, *phone* -> phone, *name* -> person name, *address* -> address, *ip* -> ipv4 +schema +inference
(A) Detect column constraints: min/max from CHECK, regex patterns from CHECK, enum values from type definition or CHECK IN() +schema +inference
(A) Build FK dependency graph: topological sort tables so parent tables generate before children (no orphan FK refs) +schema +graph
(A) Detect circular FK dependencies and break cycles by deferring one FK edge with user confirmation +schema +graph
(A) Implement Faker-based generator: map each column to appropriate Faker provider based on detected semantic type +gen +faker
(A) Support Faker locales: allow user to select locale (en_US, de_DE, ja_JP, etc.) for culturally appropriate fake data +gen +faker
(A) Handle unique constraints in Faker generator: track generated values in set, retry on collision up to 100x then error +gen +faker
(A) Handle NOT NULL constraints: never emit NULL for non-nullable columns, respect default values from schema +gen +faker
(A) Handle CHECK constraints: validate generated values against parsed CHECK expressions, retry if violated +gen +faker
(A) Implement FK value resolver: for child table FK columns, sample from already-generated parent table PK values uniformly +gen +fk
(A) Allow user to specify FK distribution: uniform, zipf (skewed), or weighted by parent row attributes +gen +fk
(A) Implement CTGAN generator using sdv SingleTableSynthesizer: train on user-provided data sample, generate synthetic rows +gen +gan
(A) Configure CTGAN hyperparams with sensible defaults: epochs=300, batch_size=500, generator/discriminator dims=(256,256) +gen +gan
(A) Implement training progress callback: emit epoch, generator_loss, discriminator_loss to TUI progress bar +gen +gan
(A) Implement TVAE generator using sdv TVAESynthesizer as alternative to CTGAN for smaller datasets +gen +tvae
(A) Implement Gaussian Copula generator using sdv GaussianCopulaSynthesizer for fast statistical-distribution-preserving generation +gen +copula
(A) Build schema-only mode: generate data purely from schema structure + semantic inference without requiring real data sample +gen +schemaonly
(A) Build data-seeded mode: accept CSV/Parquet/DB query as real data sample, fit generative model, produce synthetic output +gen +seeded
(A) Implement mode toggle in config: schema_only=true uses Faker+constraints, schema_only=false trains GAN/Copula/TVAE on sample +core
(A) Implement multi-table generation orchestrator: iterate topological order, generate each table, pass PK values downstream for FK resolution +gen +orchestrator
(A) Support configurable row counts per table via TUI or CLI (default 1000, max 10000) +gen +config
(A) Design main TUI layout: left pane (table list + schema tree), center pane (config/preview), right pane (generation status), bottom bar (keybinds) +tui
(A) Implement DB connection dialog: form with host, port, database, user, password fields, test connection button +tui +connect
(A) Add DDL file picker alternative: file browser to select .sql file instead of live connection +tui +connect
(A) Implement schema explorer panel: tree view of tables -> columns with type, nullable, PK/FK badges +tui +schema
(A) Highlight FK relationships in schema tree: show arrows or linked table references inline +tui +schema
(A) Implement per-table config panel: select generation method (dropdown), row count (input), FK mode (radio), locale (dropdown) +tui +config
(A) Implement global config panel: set defaults for method, row count, locale, seed that apply to unconfigured tables +tui +config
(A) Add "select all / deselect all" toggle for tables to include in generation +tui +config
(A) Implement generation progress view: per-table progress bar, current table name, ETA, rows generated / total +tui +progress
(A) Show GAN training progress in real-time: epoch counter, loss curves as sparkline, estimated time remaining +tui +progress
(A) Implement data preview panel: show first 20 rows of generated data for selected table in scrollable data table widget +tui +preview
(A) Allow re-generation of single table from preview panel (r key) without re-running entire pipeline +tui +preview
(A) Implement export dialog: select format (SQL INSERT, CSV, Parquet, direct DB insert), choose output path or target DB connection +tui +export
(A) Implement SQL INSERT exporter: emit INSERT statements batched per 500 rows with proper escaping and quoting +export +sql
(A) Implement CSV exporter: write per-table CSV files with headers to user-specified directory +export +csv
(A) Implement direct DB insert exporter: use SQLAlchemy bulk_insert_mappings to write generated rows to target DB +export +db
(A) Add transaction wrapping for direct DB insert: BEGIN, insert all tables in FK order, COMMIT; ROLLBACK on error +export +db
(A) Implement keyboard nav: j/k navigate tables, enter select, tab cycle panels, g generate, e export, q quit, ? help +tui
(A) Build help overlay (? key) listing all keybindings and workflow steps +tui
(A) Build CLI entry point: `schema-morph --dsn <conn_string> | --ddl <file.sql> [--method <gan|copula|tvae|faker>] [--rows <n>] [--output-dir <path>] [--format <sql|csv|parquet|db>]` +cli
(A) Add `--no-tui` flag for headless batch mode: parse schema, generate, export, print summary to stdout +cli
(A) Add `--seed <int>` flag for reproducible generation across runs +cli
(A) Validate CLI inputs: check DSN is connectable or DDL file exists, method is valid, rows within 1-10000 range +cli
(A) Wire CLI -> schema introspect -> config -> generation -> export pipeline with error propagation +core
(A) Write unit test: schema introspector correctly reflects PK, FK, unique constraints from mock PostgreSQL schema +test +schema
(A) Write unit test: topological sort produces valid FK-respecting generation order for 5-table schema with diamond deps +test +graph
(A) Write unit test: Faker generator respects NOT NULL, unique, and CHECK constraints over 1000 rows +test +faker
(A) Write unit test: FK resolver produces valid references (all child FK values exist in parent PK set) +test +fk
(A) Write integration test: full pipeline from DDL parse -> Faker generate -> CSV export -> re-import produces valid schema +test +integration
(A) Write integration test: CTGAN train on 500-row sample -> generate 1000 rows -> verify column dtype match +test +gan
(B) Implement MySQL connection handler and MySQL-specific type mappings (TINYINT, ENUM, SET, MEDIUMTEXT) +db +mysql
(B) Implement SQLite connection handler and SQLite type affinity mapping (TEXT, INTEGER, REAL, BLOB) +db +sqlite
(B) Implement MSSQL connection handler with pyodbc and MSSQL-specific types (NVARCHAR, UNIQUEIDENTIFIER, BIT) +db +mssql
(B) Implement MariaDB connection handler inheriting MySQL handler with MariaDB-specific extensions (INET6, UUID) +db +mariadb
(B) Support schema filtering: allow user to select specific schema/namespace (PostgreSQL schemas, MySQL databases) +schema +filter
(B) Detect and handle composite primary keys in FK resolution: generate tuple-consistent references +gen +fk
(B) Detect and handle composite unique constraints: ensure generated row tuples are unique across specified column sets +gen +constraints
(B) Handle self-referential FKs (e.g., employee.manager_id -> employee.id): generate in two passes, backfill refs +gen +fk
(B) Implement JSON/JSONB column generator: produce structurally consistent JSON based on sample data analysis or user-provided JSON schema +gen +json
(B) Implement array column generator (PostgreSQL arrays): produce typed arrays matching element type constraints +gen +postgres
(B) Implement enum column generator: sample from declared enum values with configurable distribution +gen +types
(B) Implement date range constraint support: generate temporal values within user-specified or CHECK-derived min/max bounds +gen +temporal
(B) Implement cross-column correlation preservation in Faker mode: e.g., city matches state matches zip via locale-aware provider +gen +correlation
(B) Implement conditional column generation: if column B depends on column A value (e.g., status -> status_date), allow user-defined rules +gen +rules
(B) Implement Parquet exporter with proper schema typing (pyarrow) for analytics pipeline consumption +export +parquet
(B) Implement target DB export with schema auto-creation: CREATE TABLE if not exists on target DB before insert +export +db
(B) Add `--truncate-target` flag: TRUNCATE target tables before inserting synthetic data +cli +export
(B) Implement privacy validation layer: compute distance metrics between real sample and synthetic data +privacy
(B) Calculate Nearest Neighbor Distance Ratio (NNDR): flag synthetic rows too close to real records (memorization risk) +privacy +nndr
(B) Calculate column-wise statistical distance (Jensen-Shannon divergence) between real and synthetic distributions +privacy +stats
(B) Calculate pairwise correlation matrix difference between real and synthetic data, flag significant deviations +privacy +stats
(B) Display privacy report in TUI: per-table privacy score, flagged high-risk rows, distribution comparison histograms +tui +privacy
(B) Implement privacy threshold config: --min-privacy-score <0-1> fails generation if synthetic data too similar to real +cli +privacy
(B) Add data quality report panel in TUI: null rate, unique rate, value distribution histogram per column for generated data +tui +quality
(B) Implement saved profiles: persist generation config (method, rows, locale, FK mode per table) to ~/.config/schema-morph/profiles/<name>.toml +config
(B) Add profile selector in TUI: load/save/delete named profiles for repeat generation against same schema +tui +config
(B) Implement connection string history: remember last 10 DSNs in ~/.config/schema-morph/history.toml, show in connection dialog +tui +config
(B) Add column-level override in TUI: let user manually set Faker provider, regex pattern, or value list for specific columns +tui +config
(B) Add seed data import: user provides partial CSV with some columns filled, generator fills remaining columns +gen +partial
(B) Implement generation method auto-recommendation: suggest GAN for >200 sample rows, Copula for 50-200, Faker for schema-only +gen +autosuggest
(B) Write unit test: MySQL type mapper correctly handles ENUM, SET, TINYINT(1)->bool conversion +test +mysql
(B) Write unit test: privacy NNDR correctly flags memorized rows when synthetic == real sample subset +test +privacy
(B) Write unit test: composite PK FK resolution generates valid tuple references +test +fk
(B) Write integration test: PostgreSQL live schema reflect -> CTGAN train -> direct DB insert -> SELECT COUNT matches +test +integration
(B) Write benchmark: Faker generation throughput at 1k/5k/10k rows for 20-column table +test +perf
(C) Implement Oracle DB connection handler with cx_Oracle and Oracle-specific types (NUMBER, VARCHAR2, CLOB) +db +oracle
(C) Implement CockroachDB connection handler inheriting PostgreSQL with CRDB-specific serial/UUID handling +db +cockroach
(C) Implement schema diff detection: compare current schema to previous run, highlight added/removed/changed columns +schema +diff
(C) Implement time-series aware generation: detect temporal ordering columns and generate monotonically increasing timestamps +gen +timeseries
(C) Implement geographic data generation: PostGIS GEOMETRY/GEOGRAPHY columns generate valid WKT points within bounding box +gen +geo
(C) Implement LLM-assisted column inference: send ambiguous column names to local Ollama model for semantic type classification +gen +llm
(C) Add data masking mode: instead of full synthesis, apply k-anonymity / l-diversity transforms to real data in-place +privacy +masking
(C) Implement synthetic data versioning: hash generation config + schema, tag output files for reproducibility tracking +export +version
(C) Add TUI theme configuration via ~/.config/schema-morph/theme.toml with dark/light/accessible presets +tui +config
(C) Implement plugin system: allow user-defined Python generator functions for custom column types via entry_points +gen +plugin
(C) Add GDPR compliance report export: document which columns contained PII, what synthesis method was used, privacy scores +export +compliance
(C) Write E2E test: launch TUI headlessly, simulate full workflow (connect -> configure -> generate -> export), validate output files +test +e2e
(C) Write fuzz test: feed malformed DDL SQL to parser, verify no crashes and graceful error messages +test +fuzz
